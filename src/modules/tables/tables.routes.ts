// import { Router } from 'express';
// import { getTablesController, closeTableController, getTableByIdController, setGuestsController} from './tables.controller';
// import { asyncHandler } from '../../middlewares/asyncHandler';
// import { validateOpenTable } from '../../middlewares/validateOpenTable';

// const router = Router(); 

// // router.post('/open', validateOpenTable, asyncHandler(openTableController));
// // router.get('/cashier/:userId', asyncHandler(getTablesForCashierController))
// router.get('/', asyncHandler(getTablesController));
// router.post('/close', validateOpenTable, asyncHandler(closeTableController));
// router.get('/:id', asyncHandler(getTableByIdController));
// router.post('/:id/guests', asyncHandler(setGuestsController));

// export default router 

